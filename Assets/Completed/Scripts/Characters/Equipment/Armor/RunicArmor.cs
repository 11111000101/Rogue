using UnityEngine;
using System.Collections;

public class RunicArmor : IArmor {

	// Use this for initialization
	void Start () {
        ap = 25;
        time = 5;
        armor = 10;

        // Defensive (F): Build up a Shield absorbing 50dmg for 20s
    }

    // Update is called once per frame
    void Update () {
	
	}
}
